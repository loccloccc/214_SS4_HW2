spring:
    application:
        name: pharmacy-service

    config:
        import: optional:configserver:http://localhost:8888

    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3311/pharmacy_db
        username: root
        password: duongloc2505

    jpa:
        show-sql: true
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                format_sql: true
                dialect: org.hibernate.dialect.MySQLDialect
server:
    port: 8085